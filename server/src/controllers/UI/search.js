// import { Product, Variants } from '../../models/index.js';

// search GET
export const searchGet = (req, res) => {
    res.json({message: 'Placeholder for searchGet. Will retrieve products based on search criteria entered in the request body '})
}