// import { Variant } from '../../models';

// Variant GET
export const variantGet = (req, res) => {
    res.json({message: `Placeholder for variantGet. Will retrieve a specific variant based on the ID ${req.params.id} entered as a param for api/products/variant/:id`});
}