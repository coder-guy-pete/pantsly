// import { Order, OrderDetails } from '../../models/index.js';

// Order GET
export const orderGet = async (req, res) => {
    res.json({message: `Placeholder for orderGet. Will likely be used to retrieve orders by ID ${req.params.id} entered as a param for api/orders/order/:id`})
};